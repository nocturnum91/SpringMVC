<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nocturnum.mapper.BoardMapper">

    <select id="read" resultType="BoardVO">
        select bno, title, content, writer, regdate, updatedate from tb_board where bno = #{bno}
    </select>

    <select id="getList" resultType="BoardVO">
        select bno, title, content, writer, regdate, updatedate from tb_board where bno > 0
    </select>

    <insert id="insert">
        insert into tb_board (bno, title, content, writer) values (0, #{title}, #{content}, #{writer})
    </insert>

    <insert id="insertSelectKey">
    <selectKey keyProperty="bno" order="BEFORE" resultType="long">
        select auto_increment from information_schema.TABLES where TABLE_SCHEMA = 'noc' and TABLE_NAME = 'tb_board'
    </selectKey>
        insert into tb_board (bno, title, content, writer) values (#{bno}, #{title}, #{content}, #{writer})
    </insert>

    <update id="update">
        update tb_board set title = #{title},
                            content = #{content},
                            writer = #{writer},
                            updateDate = now()
                            where bno = #{bno}
    </update>

    <delete id="delete">
        delete from tb_board where bno = #{bno}
    </delete>

</mapper>